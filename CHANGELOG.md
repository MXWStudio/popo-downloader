# 更新记录

## v0.7.0-beta.8（2026-08-03）

- 修复下载过程中打开扩展弹窗时，连接检查可能覆盖较新的任务状态，导致进度停在一个 Gopeed 已完成的文件上的问题。
- 后台再次运行时会自动恢复遗留在“准备中”的文件，不再永久停住。
- 新增弹窗轮询不写入任务状态、准备步骤中断自动接续的回归测试。

## v0.7.0-beta.7（2026-08-03）

- 修复旧版本保存过的文件格式或关键词筛选仍在后台生效、导致文件夹内其他类型文件被跳过的问题。
- 文件夹下载现在固定递归处理所有用户文件，不限制扩展名，也包含无扩展名文件；仅忽略系统自动生成的元数据文件。
- 新增升级场景与混合文件格式回归测试。

## v0.7.0-beta.6（2026-08-03）

- 修复取消任务重新打开后仍停在等待状态的问题；取消会立即结束 POPO 队列记录，同时保留已经交给 Gopeed 的文件，并继续下一个排队任务。
- 已完成和已取消任务不再长期显示在扩展弹窗中，刷新网页或重新打开扩展后只保留进行中的任务。
- 尚未解决的失败仍会保留重试入口；同一文件夹重新下载或重试后，旧失败提示会自动被新结果替代。
- 修复扩展后台短暂中断后，旧连接报错一直留在弹窗中的问题。

## v0.7.0-beta.5（2026-08-03）

- 修复项目数被插入右侧排序工具栏、导致显示在工具栏上方的问题。
- 项目数现在固定显示在左侧“所有类型”筛选按钮右边，并保持右侧排序工具栏原有的单行布局。
- 新增定位锚点与左侧自动占位检查，防止后续改动再次把项目数推到页面顶部。
- 绿色包 SHA-256：`42ce99e5c853bbdc14e480566aedd3f9fc35ca6758288d81ba066a5ee0cd8b1f`。

## v0.7.0-beta.4（2026-08-03）

- 修复文件夹页缺少当前目录项目总数、用户无法在下载前判断目录规模的问题。
- 在“按照目录顺序”左侧显示当前第一层“文件 + 文件夹”的项目数；虚拟大列表通过行号与占位高度计算，不滚动用户页面。
- 移除“发现 N 个文件，确认下载？”手动确认窗口；点击下载后直接入队，扫描完成后自动连接 Gopeed 开始下载。
- 队列新增“当前层项目数”口径，并将递归发现的文件和文件夹分开显示，避免数量含义混淆。
- 空目录、SPA 目录切换、34 项虚拟大列表、直接入队与无确认弹窗均纳入自动化和人工验收。
- 绿色包 SHA-256：`245dc3e7d9a94a4d9a32fbe8e89359648b1cb6754633feff9eb6ed8cf93e226d`。

## v0.7.0-beta.3（2026-08-03）

- 修复把 POPO 页面空间短码误当作真实团队空间 ID，导致预览未直接暴露媒体地址时批量出现“下载未建立”的问题。
- 单文件地址接口未返回地址时会短暂重试 3 次，并在最终失败信息中保留 HTTP、接口代码和消息，便于继续定位。
- 刷新临时地址时若 Gopeed 原任务已不存在，自动建立新任务，不再反复返回 `task not found`。
- 原 POPO 标签页关闭后，可在重新打开的同一团队空间页面自动接管未完成任务，不再永久停在等待状态。

## v0.7.0-beta.2（2026-08-03）

- 左下角队列改为默认折叠，只显示进行中任务；完成和失败历史仅在扩展弹窗保留。
- 页面刷新后自动重建隐藏工作区；已开始的 Gopeed 下载继续，正在准备的文件退回队列且不计失败次数。
- 页面队列和扩展弹窗增加文件进度条，固定扩展图标显示百分比、排队数或失败提醒。
- 修复队列按钮与全页 DOM 监听互相触发、导致 POPO 文件列表消失的问题。
- 绿色包 SHA-256：`1505235bce37e3080c502ad3aad158b6a6660322a71353e6a7f4c621f8905f81`。

## v0.7.0-beta.1（2026-08-03）

- 支持连续添加多个母文件夹，并在页面左下角和扩展弹窗显示下载队列。
- 同一文件夹重复点击不会创建重复任务，会提示当前处理或排队位置。
- 每个任务可以取消未开始文件；已经交给 Gopeed 的文件会保留并继续完成。
- 修复取消与异步下载准备并发时旧状态覆盖新状态、旧任务继续复活的问题。
- 大文件列表改为每 200 项分块保存，并申请大容量本地存储权限。
- 增加万级文件统计、三任务排队和安全取消测试。
- 绿色包 SHA-256：`6c88a3c8948c05905c69bffd0b3b7ff7aff86a187729a6693d64cd91a2779890`。

## v0.6.0-beta.3（2026-08-03）

- 提供 Windows x64 绿色 ZIP，自带 Gopeed v1.9.3 和首次配置助手。
- 固定 Chrome 扩展 ID，后续更新可覆盖原运行目录。
- 下载任务固定并发 5，支持暂停、继续、取消和失败重试。
- 自动测试 32/32 通过，隔离验证确认包内 Gopeed 可正常启动。
- 绿色包 SHA-256：`6ccb45700f7462f20d530f508fa3d06e770be4c5e9e3ac21e02da2cfdc3b175c`。

首次使用需解压 ZIP、运行 `POPO-Setup.exe`，并在 Chrome 中手动加载一次扩展。
