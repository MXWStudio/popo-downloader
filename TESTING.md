# POPO 稳定下载助手 0.7.2 正式绿色版

发布日期：2026-08-12

测试包已经包含扩展、Gopeed v1.9.3 便携版、Windows 本机助手和绿色安装助手。不需要管理员权限，不需要另外安装 Gopeed，也不需要配置 API 端口。

## 第一次使用

1. 解压整个 ZIP，不要直接在 ZIP 内运行程序。
2. 双击 `POPO-Setup.exe`（绿色版安装助手）。
3. 选择安装位置；默认优先建议空间充足的非系统盘，也可点击“浏览”选择其他位置。
4. 安装助手会自动复制运行文件、注册本机助手，并且只打开准备好的 `Extension` 文件夹，不会自动打开 Chrome 扩展管理页。
5. 用户自行打开 Chrome 扩展管理页，开启“开发者模式”，点击“加载已解压的扩展程序”。
6. 选择安装助手打开的 `Extension` 文件夹。添加完成后即可使用，不需要重新加载。

再次运行安装助手会自动识别现有正式版：同版本默认执行修复，较新版本执行覆盖更新；选择新位置则执行跨盘迁移。三种操作都会保留下载记录和设置，自动更新也会继续沿用保存的位置。

下载引擎只在需要时后台启动。安装助手不会关闭 Chrome，也不会修改企业策略，因此 Chrome 不会显示“由组织管理”。

## 从旧测试版升级

`0.6.0-beta.3` 开始使用固定扩展 ID。已安装 `0.7.1` 正式版的电脑可以覆盖更新到 `0.7.2`；更新后需要在扩展管理页点击一次“重新加载”，并刷新 POPO 页面。

新版安装助手会先把扩展、NativeHost 和 Gopeed 复制到独立候选目录，核对清单版本、固定扩展 ID、必需文件及逐文件 SHA-256；全部通过后才切换固定安装路径。切换失败会自动恢复旧目录和旧安装状态，成功后保留上一版本回滚快照。

覆盖、修复或迁移若需要替换正在运行的下载服务，会提示先从系统托盘退出 Gopeed；退出后重新运行即可，下载记录不会删除。

## 建议测试顺序

1. 打开扩展弹窗，确认版本为 `0.7.2`；展开“设置”，确认下载服务和保存位置可用。
2. 重试此前接口提示“没有权限操作”的双文件素材文件夹，确认两个文件都能交给 Gopeed，页面不再显示未完成。
3. 一键下载时让首个文件夹存在未完成项，确认该文件夹单独记录失败，后续文件夹仍会自动继续扫描和下载，不会整批暂停。
4. 点击“选择文件夹”，确认 Windows 系统文件夹选择窗口正常出现。
5. 打开一个含 5 个文件的文件夹，确认“所有类型”右侧同一行显示“5 个项目”，而不是出现在排序工具栏上方；打开空目录确认显示“0 个项目”。
6. 打开一个含文件和子文件夹的大目录，确认项目数等于当前第一层的文件与文件夹总和，页面列表不会自动滚动。
7. 点击一个含 1 个小文件的文件夹下载按钮，确认直接加入队列，不再出现“确认下载”弹窗。
8. 下载一个同时包含视频、图片、压缩包、文档、设计源文件和无扩展名文件的文件夹，确认所有用户文件都进入 Gopeed；同时确认 Gopeed 最多运行 5 个任务。
9. 下载一个含 20 个以上视频的文件夹。
10. 核对成功数、失败明细、文件名和原目录结构。
11. 连续点击三个母文件夹，确认左下角出现一个处理任务和两个排队任务。
12. 重复点击同一母文件夹，确认只保留一个任务并提示已添加。
13. 在一个任务已有文件下载时点击“取消未开始文件”，确认等待文件取消、已交给 Gopeed 的文件没有被删除，并且已取消任务显示“恢复未开始文件”按钮。
14. 下载过程中刷新 POPO 页面，确认已开始的 Gopeed 下载不中断，准备中的文件稍后自动接续且失败数不增加。
15. 对此前提示“没有取得可由扩展管理的单文件下载地址”的文件执行失败重试，确认扩展解析真实团队空间 ID 后把单文件地址交给 Gopeed。
16. 在 Gopeed 中删除一个等待刷新地址的原任务后重试，确认扩展自动新建任务，不再显示 `task not found`。
17. 下载尚未完成时关闭原 POPO 标签页，再打开任意 POPO 团队空间页面，确认隐藏工作区和剩余任务自动恢复。
18. 确认左下任务条默认折叠，点击“展开/收起”可切换；打开右上扩展弹窗时左下自动收起，没有活动任务时自动隐藏。
19. 确认右上弹窗负责完整任务管理、左下只显示持续摘要、右下只显示完成或失败等一次性事件；扫描、准备、排队和下载中不在右下重复提示，排队任务不显示动态进度条。
20. 误取消一个仍有未开始文件的任务并重新打开扩展，点击“恢复未开始文件”，确认只重新处理取消部分，已成功文件不重复下载。
21. 完成一个无失败文件的任务并刷新网页、重新打开扩展，确认结束任务自动消失。
22. 对失败文件重新下载；恢复正常后确认旧失败卡片、失败明细和临时连接报错都自动消失。
23. 在下载过程中持续打开扩展弹窗，确认连接状态刷新不会让进度停在某个已完成文件；如后台短暂中断，确认该文件自动回到队列并继续后续下载。
24. 暂停任务、关闭原 POPO 页面、在另一个团队空间页面点击继续，确认扩展会主动重建工作区并继续向 Gopeed 提交文件。

## 反馈时请提供

- 扩展版本和 Chrome 版本。
- POPO 文件夹内的文件数量及是否包含子文件夹。
- 成功/失败数量和失败明细截图。
- Gopeed 任务页截图。
- 不要发送 POPO 登录信息、Cookie、下载地址或公司敏感素材。

## 已知限制

- 普通 Windows Chrome 不允许本地程序静默添加扩展，因此第一次仍需用户点击“加载已解压的扩展程序”。
- 0.7.0 以前的测试版没有自动更新能力，需要手动安装一次正式版。
- 正式更新对象通过 HTTPS 公网读取；存储桶目录和其他对象仍保持私有，客户端同时校验 RSA-SHA256 发布签名与文件 SHA-256。
- 有活动下载任务时自动延后更新；更新包校验或安装失败时保留当前版本并记录失败状态。
- 公司安全策略可能拦截未知的未签名 EXE，需要用户确认允许运行。
