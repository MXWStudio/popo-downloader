# POPO 稳定下载助手 0.6.0-beta.3 绿色测试版

发布日期：2026-08-01

测试包已经包含扩展、Gopeed v1.9.3 便携版、Windows 本机助手和绿色安装助手。不需要管理员权限，不需要另外安装 Gopeed，也不需要配置 API 端口。

## 第一次使用

1. 解压整个 ZIP，不要直接在 ZIP 内运行程序。
2. 双击 `POPO-Setup.exe`（绿色版安装助手）。
3. 安装助手会自动复制运行文件、注册本机助手，并打开 Chrome 扩展管理页和准备好的 `Extension` 文件夹。
4. 在 Chrome 开启“开发者模式”，点击“加载已解压的扩展程序”。
5. 选择安装助手打开的 `Extension` 文件夹。添加完成后即可使用，不需要重新加载。

安装助手会把运行文件放在：

`%LOCALAPPDATA%\POPOStableDownloader`

下载引擎只在需要时后台启动。安装助手不会关闭 Chrome，也不会修改企业策略，因此 Chrome 不会显示“由组织管理”。

## 从旧测试版升级

`0.6.0-beta.3` 开始使用固定扩展 ID。首次升级时，请在 `chrome://extensions/` 删除旧测试扩展，然后按照上面的“第一次使用”重新添加一次。后续绿色版升级只需运行新版安装助手，并在扩展管理页点击一次“重新加载”。

## 建议测试顺序

1. 打开扩展弹窗，确认版本为 `0.6.0-beta.3` 且 Gopeed 显示“已连接”。
2. 点击“选择文件夹”，确认 Windows 系统文件夹选择窗口正常出现。
3. 下载一个含 1 个小文件的 POPO 文件夹。
4. 下载一个含 5 个视频的文件夹，确认 Gopeed 最多同时运行 5 个任务。
5. 下载一个含 20 个以上视频的文件夹。
6. 核对成功数、失败明细、文件名和原目录结构。

## 反馈时请提供

- 扩展版本和 Chrome 版本。
- POPO 文件夹内的文件数量及是否包含子文件夹。
- 成功/失败数量和失败明细截图。
- Gopeed 任务页截图。
- 不要发送 POPO 登录信息、Cookie、下载地址或公司敏感素材。

## 已知限制

- 普通 Windows Chrome 不允许本地程序静默添加扩展，因此第一次仍需用户点击“加载已解压的扩展程序”。
- 绿色测试版不会自动更新；更新后需要在 Chrome 扩展页点击一次“重新加载”。
- 公司安全策略可能拦截未知的未签名 EXE，需要用户确认允许运行。
